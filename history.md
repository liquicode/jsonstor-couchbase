# jsonstor-couchbase
[`@liquicode/jsonstor-couchbase`](https://github.com/liquicode/jsonstor-couchbase)


# Project History


v0.2.0 (current)
---------------------------------------------------------------------

***First release.***

The adapter for Couchbase Server, through its query service with no driver. Tested on
  Couchbase 5.0 and 8.0.

- Built on `@liquicode/jsonstor` 0.2.0 and `@liquicode/jsongin` 0.2.0. A criteria the engine
  refuses is refused before the storage acts on it.
- Declares Node.js `>=18.0.0` in `engines`.
